# ─────────────────────────────────────────────────────────────
# requirements.txt
#
# Install with:   pip install -r requirements.txt
# Then run:       python main.py
#
# WHY SELENIUM INSTEAD OF PLAYWRIGHT:
#   Playwright needs 'greenlet' which requires Microsoft Visual C++ 14.0
#   to compile on Python 3.13+. Selenium has pre-built wheels for ALL
#   Python versions — zero compilation, zero C++ needed.
# ─────────────────────────────────────────────────────────────

selenium==4.21.0          # browser automation — pre-built wheels, works on Python 3.13+
beautifulsoup4==4.12.3    # HTML parser — extracts data from page content
rich==13.7.1              # beautiful terminal output — tables, colors, progress